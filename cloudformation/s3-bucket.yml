AWSTeplateFormatVersion: '2010-09-09'
Description: 'S3 bucket with enviroment tagging'

Parameters:
  Enviroment:
    Types: String
    Default: test
    AllowedValues:
      - test
      - stagiing
      - production

Resources:
  MyS3Bucket:
    Type: 'AWS::S3::Bucket'
    Properties:
      BucketName: 'steves-unique-bucket'
      Tags:
        - Key: Enviroment
          Value: !Ref Enviroment
        - Key: Project
          Value: GithubActionsTesting

  Outputs:
    BucketName:
      Desvription: 'Name of the created S3 bucket'
      Value: !Ref MyS3Bucket


